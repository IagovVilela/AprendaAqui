// Cole este campo no model User. É anulável de propósito:
// usuários atuais ficam com NULL e o app usa o avatar padrão.
// Não remova nem altere colunas existentes.

model User {
  // ...campos existentes...
  avatarConfig Json? @map("avatar_config")
  // ...relações existentes, incluindo inventory UserInventoryItem[]...
}
